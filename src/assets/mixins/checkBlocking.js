export const checkBlocking = {
    methods: {
        checkBlocking(jobBloquea){
            if (jobBloquea == false ){
              //console.log(jobBloquea)
              return true
            } else {
              return false
            }
          }
    },
}
